spring:
  profiles:
    active: dev  #${spring.profile.active}

  application:
    name: consumer-user  #${prefix}

  cloud:
    nacos:
      config:
        server-addr: localhost:80
        file-extension: yaml  #${file-extension}
        group: DEFAULT_GROUP
        namespace: a3c2f359-9c40-4bb4-88e0-d4bb5ed38780
